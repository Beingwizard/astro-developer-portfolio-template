---
import { Code } from 'astro:components';

const code = `Community Copilot {
    name: Lakshit Pant;
    skills: Bluffing around with code;
    interests: Community & Initiatives, Advocacy with no bs, Experimentation with everything;
};

interface TechStack {
    devOps: string[];
    backend: string[];
   frontend: string[];
};

type ContactType = 'github' | 'twitter' | 'linkedIn' | 'email';

const techStack: TechStack = {
    devOps: [
        'GitHub',
        'Cloudflare',
        'Docker'
    ],
    frontend: [
        'React',
        'JavaScript',
        'TailwindCSS',
        'Astro',
        'Ionic',
        'Stencil',
        'SCSS',
        'Markdown',
        'WordPress'
    ],
    backend: [
        'Supabase',
        'Node.js',
        'PostgreSQL',
        'MySQL',
        'S3 Storage',
        'Resend'
    ]
};

// Developer profile
const me: Developer = {
    name: 'Lakshit Pant',
    skills: [
        ...techStack.devOps,
        ...techStack.backend,
        ...techStack.frontend,
        ...techStack.design,
        ...techStack.CX
    ],
    interests: [
        'Program Management',
        'Linux',
        'Performance',
        'Self-Hosting',
        'Networking'
    ]
};

// Contact function
const contact = (type: ContactType): string => {
    switch (type) {
        case 'github':
            return 'https://github.com/beingwizard';
        case 'twitter':
            return 'https://x.com/wizard_locks';
        case 'linkedIn':
            return 'https://www.linkedin.com/in/laxitpant';
        case 'email':
            return 'lakshitlucky1234@gmail.com';
        default:
            return 'Send fax.';
    }
};

`;
---

<Code
    code={code}
    lang="js"
    wrap={true}
/>

<style is:global>

    /* Custom styling for code block line numbers */
    pre.astro-code {
        @apply bg-transparent !important;
    }

    pre.astro-code code {
        @apply block;
        counter-reset: step;
        counter-increment: step 0;
    }

    pre.astro-code code .line {
        @apply relative;
    }

    pre.astro-code code .line::before {
        content: counter(step);
        counter-increment: step;
        @apply text-gray-600 pr-2;
    }

</style>